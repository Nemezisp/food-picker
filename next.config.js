/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "fastly.4sqi.net", "edamam-product-images.s3.amazonaws.com"],
  }
}

module.exports = nextConfig
